>>> Are there a way to get artifacts or files from a docker-based gitlab job? Ho
... w can I use docker volume if I do not have administrative rights to the runn
... er? Is there an alternative way to get files out of the job?
... 
... Please provide me with a small Node.js-based project. This project is a REST
...  API service with one endpoint.
... This endpoint accepts a GET request with id as a path parameter and returns 
... a JSON response of a user like {id:uuid, first name, last name, age}.
... There should be some collection of users the service can return on a GET req
... uest.
... 
... There should also be tests, for example using Jest or Vitest, that test the 
... endpoint described above.
... One of these tests should read user information from the service and save it
...  into a file in some folder.
... 
... Both the service and tests should work from a docker container.
... 
... There should be Gitlab workflow consisting of two jobs: build (stage build) 
... and test (stage test).
... The 'build' job performs code analysis and builds the docker image.
... The 'test' job runs tests and service in this image.
... There should be artifacts or anything the 'test' job leaves after its end. T
... he user information file should be available after the pipeline is finished.
... 
... 
... Please generate enough code to make the solution working without a lot of wo
... rk. The answer should be complete.
... 
Certainly! Below is a complete Node.js-based project that includes a REST 
API service, tests using Jest, and a GitLab CI/CD pipeline with two jobs: 
`build` and `test`.

### Project Structure

```
my-nodejs-app/
├── Dockerfile
├── .gitlab-ci.yml
├── package.json
├── src/
│   ├── app.js
│   └── users.json
└── tests/
    └── api.test.js
```

### Files and Code

#### `package.json`

```json
{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "A simple REST API service with user data.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}
```

#### `Dockerfile`

```dockerfile
# Use an official Node runtime as a parent image
FROM node:14

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy package.json and install dependencies
COPY package*.json ./
RUN npm install

# Bundle app source inside Docker image
COPY src ./src

# Make port 3000 available to the world outside this container
EXPOSE 3000

# Define environment variable
ENV NODE_ENV production

# Run app.js when the container launches
CMD ["node", "src/app.js"]
```

#### `src/app.js`

```javascript
const express = require('express');
const app = express();
const port = 3000;

// Sample user data
const users = {
    "1": { id: "1", firstName: "John", lastName: "Doe", age: 28 },
    "2": { id: "2", firstName: "Jane", lastName: "Smith", age: 34 }
};

app.get('/user/:id', (req, res) => {
    const userId = req.params.id;
    if (users[userId]) {
        res.json(users[userId]);
    } else {
        res.status(404).send('User not found');
    }
});

app.listen(port, () => {
    console.log(`App listening at http://localhost:${port}`);
});
```

#### `src/users.json`

```json
{
  "1": { "id": "1", "firstName": "John", "lastName": "Doe", "age": 28 },
  "2": { "id": "2", "firstName": "Jane", "lastName": "Smith", "age": 34 }
}
```

#### `tests/api.test.js`

```javascript
const request = require('supertest');
const app = require('../src/app');

describe('GET /user/:id', () => {
    it('should return the user with id 1', async () => {
        const response = await request(app).get('/user/1');
        expect(response.statusCode).toBe(200);
        expect(response.body).toEqual({ id: '1', firstName: 'John', 
lastName: 'Doe', age: 28 });
    });

    it('should return the user with id 2', async () => {
        const response = await request(app).get('/user/2');
        expect(response.statusCode).toBe(200);
        expect(response.body).toEqual({ id: '2', firstName: 'Jane', 
lastName: 'Smith', age: 34 });
    });

    it('should save user information to a file', async () => {
        const response = await request(app).get('/user/1');
        expect(response.statusCode).toBe(200);

        // Write the response body to a file
        const fs = require('fs');
        fs.writeFileSync('./test-results/user_info.json', 
JSON.stringify(response.body, null, 2));

        // Check if the file is written successfully
        const data = fs.readFileSync
